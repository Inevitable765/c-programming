#include<stdio.h>

int main(){
    // •	Declare three variables[Give them some name]
    int first, second, third;
    first = 5,
    second = 6;
    third = 7;
    printf("value of first variable is %d and value of the second variable is %d and value of the third variable is %d", first, second, third);
    return 0;
}